import random

data = []

for i in range(300):  # number of students
    studytime = random.randint(1, 4)      # 1–4 (low → high)
    failures = random.randint(0, 3)       # number of past failures
    absences = random.randint(0, 30)      # days absent
    G1 = random.randint(0, 20)            # Grade 1
    G2 = random.randint(0, 20)            # Grade 2

    # Make final grade depend on behavior (so ML can learn)
    G3 = int(
        0.3 * G1 +
        0.4 * G2 +
        2 * studytime -
        3 * failures -
        0.1 * absences +
        random.randint(-2, 2)  # noise
    )

    # keep grade in valid range
    G3 = max(0, min(20, G3))

    data.append([studytime, failures, absences, G1, G2, G3])

columns = ["studytime", "failures", "absences", "G1", "G2", "G3"]

df = pd.DataFrame(data, columns=columns)


